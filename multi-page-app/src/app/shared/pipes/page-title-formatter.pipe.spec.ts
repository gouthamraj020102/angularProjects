import { PageTitleFormatterPipe } from './page-title-formatter.pipe';

describe('PageTitleFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new PageTitleFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
